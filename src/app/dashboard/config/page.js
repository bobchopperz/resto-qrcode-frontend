"use client";

import styles from '../DashboardPage.module.css'; // Menggunakan CSS yang sama dengan DashboardPage

export default function ConfigPage() {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>Halaman Konfigurasi</h1>
      <p className={styles.content}>Konten untuk pengaturan konfigurasi akan datang di sini.</p>
    </div>
  );
}
